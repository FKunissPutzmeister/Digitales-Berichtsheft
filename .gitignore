# OS-Artefakte
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
$RECYCLE.BIN/

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Dependency-Verzeichnisse (für die Zukunft, falls Tooling dazukommt)
node_modules/

# Lokale Build-Outputs / temporäre Dateien
.tmp/
.cache/
dist/
build/

# Design-System-Experimente (gehören nicht in das Haupt-Repo)
app-NEW-DESIGN-SYSTEM/
Putzmeister Design System Package/

# Reverse-Engineering-Scratch (better-ess Bundle-Analyse, Screenshots,
# Test-Skripte) — reine Arbeitsartefakte, nicht für das geteilte Repo
.ess-reverse/

# Spezifikationen & Notizen (gehören in SharePoint, nicht ins Code-Repo)
*.docx
Fragen die vorab*.txt
temp prompt.txt
Prompt.txt

# DB-Doc-Generator (alte DB-Struktur, wird nach Azure-AD-Integration neu gemacht)
generate-db-doc.ps1

# Backend-Umgebungsvariablen (DB-Credentials, Secrets)
backend/.env

# Persistente Express-Sessions (session-file-store) — lokale Login-Cookies,
# überleben Backend-Restarts. Gehören NICHT ins Repo.
backend/sessions/

# Claude Code – lokale Agent-Worktrees & Arbeitsdateien (nicht versionieren)
.claude/
